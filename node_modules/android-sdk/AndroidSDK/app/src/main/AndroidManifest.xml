<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.jibestream.androidsdk"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:logo="@mipmap/ic_launcher"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="android.intent.action.SEARCH"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".LiveMapWithStaticMapActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTask"
            android:parentActivityName=".VenuesSelectionActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>
        <activity
            android:name=".NoInternetNoCacheActivity"
            android:noHistory="true">
        </activity>
        <activity android:name=".VenuesSelectionActivity">
        </activity>

        <service android:name=".MService"/>

        <activity android:name=".VenueDataActivity"
                  android:noHistory="true">
        </activity>
    </application>

</manifest>
